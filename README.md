# Job Portal System

Professional monorepo layout for the job portal application.

## Structure

- `backend/` - NestJS API
- `frontend/` - Vite React client

## Development

Install and run each app from its own folder.

```bash
cd backend
npm install
npm run build
```

```bash
cd frontend
npm install
npm run build
```

